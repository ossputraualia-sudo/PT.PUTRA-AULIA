PAG.Camera={async start(){const box=document.getElementById('box');box.innerHTML='<video autoplay playsinline style="width:100%"></video><button class="btn" id="take">Ambil Foto</button>';const v=box.querySelector('video');const s=await navigator.mediaDevices.getUserMedia({video:{facingMode:'environment'}});v.srcObject=s;box.querySelector('#take').onclick=async()=>{const g=await PAG.Device.gps();const c=document.createElement('canvas');c.width=1200;c.height=900;c.getContext('2d').drawImage(v,0,0,1200,900);const b=await new Promise(r=>c.toBlob(r,'image/jpeg',.82));s.getTracks().forEach(t=>t.stop());const id='DOC-'+Date.now();await PAG.Storage.put('photos',{id,blob:b,gps:g});await PAG.OfflineSync.add('dokumentasi',{id,gps:g});PAG.UI.toast('Foto tersimpan')}}};
