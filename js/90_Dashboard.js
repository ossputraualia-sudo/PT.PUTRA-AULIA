PAG.Dashboard={async render(v){const u=await PAG.Auth.ensure();if(!u)return;const m=await PAG.WebUtamaSync.getMaster();const p=(m.paket||[])[0];v.innerHTML=`<div class="hero"><small>PERSONIL LAPANGAN</small><h2>${u.name}</h2><div>${p?.nama||'Paket belum tersinkron'}</div></div><div class="grid"><button class="action" onclick="PAG.Absensi.start()">📍<br>Absensi<br><small>Selfie + GPS</small></button><button class="action" onclick="PAG.Router.go('report')">📋<br>Laporan Harian</button><button class="action" onclick="PAG.Router.go('photo')">📷<br>Dokumentasi</button><button class="action" onclick="PAG.Router.go('profile')">●<br>Profil</button></div>`}};
